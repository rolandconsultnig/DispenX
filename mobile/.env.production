# Loaded automatically for release builds (expo export / run:android --variant release / EAS production).
# Change the host if your server IP or domain changes.
EXPO_PUBLIC_API_BASE=http://13.53.33.63:4603/api
