import type { ImageSourcePropType } from 'react-native';

/** Shown in UI; launcher display name is `expo.name` in app.json. */
export const APP_DISPLAY_NAME = 'EnergyDispenX';
export const APP_TAGLINE = 'Corporate fuel management';

export const LOGO: ImageSourcePropType = require('../../assets/energydispenx-logo.png');
