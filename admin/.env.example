# Optional: path or URL to Android APK on /download (defaults to /releases/app-release.apk).
# VITE_MOBILE_APK_URL=/releases/app-release.apk

# Where Vite proxies /api in dev (default http://localhost:4601). Set if your API uses another port, e.g. PORT=4000 in server/.env:
# VITE_API_PROXY_TARGET=http://localhost:4000
